export default interface TeacherItemProps {
  teacher: Teacher;
}

export interface Teacher {
  id: number;
  avatar: string;
  bio: string;
  cost: string;
  name: string;
  subject: string;
  whatsapp: string;
}
